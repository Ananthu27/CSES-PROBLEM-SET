#include <bits/stdc++.h>

using namespace std;

// #define DEBUG 1

#ifdef DEBUG
#define log(x) cout << (x) << endl
#define logspace(x) cout << (x) << " "
#else
#define log(x)
#define logspace(x)
#endif

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    // main code START

    // main code END

    return 0;
}